# Browser Verification

When the task is UI-facing, verify the result in-browser instead of relying only on static code inspection. Use screenshots, DOM checks, or interaction evidence before declaring success.
